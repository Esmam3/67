-- Protect against duplicate runs
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

if PlayerGui:FindFirstChild("CoinGameCustomUI") then
    PlayerGui.CoinGameCustomUI:Destroy()
end

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Events = ReplicatedStorage:WaitForChild("Assets"):WaitForChild("Events")

-- --- CONFIGURATION / STATES ---
local config = {
    toggles = { AutoCollect = false, AutoSell = false, AutoLuck = false, AutoValue = false, AutoBuy = false },
    delays = { AutoCollect = 0.5, AutoSell = 2.0, AutoLuck = 1.0, AutoValue = 1.0, AutoBuy = 1.0 },
    selections = { CollectCoinType = "Basic Coin", BuyCoinType = "Copper Coin" }
}

-- --- NATIVE UI CREATION ---
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CoinGameCustomUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

-- Mobile Open/Close Toggle Button
local MenuToggle = Instance.new("TextButton")
MenuToggle.Name = "MenuToggle"
MenuToggle.Size = UDim2.new(0, 110, 0, 45)
MenuToggle.Position = UDim2.new(0, 15, 0, 15)
MenuToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
MenuToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
MenuToggle.Text = "Toggle Menu"
MenuToggle.Font = Enum.Font.SourceSansBold
MenuToggle.TextSize = 16
Instance.new("UICorner", MenuToggle).CornerRadius = UDim.new(0, 8)
MenuToggle.Parent = ScreenGui

-- Main Menu Window
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 460, 0, 280)
MainFrame.Position = UDim2.new(0.5, -230, 0.5, -140)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.Active = true
MainFrame.Draggable = true -- Mobile friendly dragging
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)
MainFrame.Parent = ScreenGui

-- Open/Close Logic
MenuToggle.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Sidebar for Tabs
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Size = UDim2.new(0, 120, 1, 0)
Sidebar.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 10)
Sidebar.Parent = MainFrame

local TabList = Instance.new("Frame")
TabList.Size = UDim2.new(1, 0, 1, -20)
TabList.Position = UDim2.new(0, 0, 0, 10)
TabList.BackgroundTransparency = 1
TabList.Parent = Sidebar
local TabLayout = Instance.new("UIListLayout")
TabLayout.Padding = UDim.new(0, 8)
TabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabLayout.Parent = TabList

-- Containers for Contents
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -130, 1, -20)
ContentFrame.Position = UDim2.new(0, 125, 0, 10)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

-- Track Created Tabs
local tabs = {}
local activeTab = nil

local function CreateTab(name)
    local TabButton = Instance.new("TextButton")
    TabButton.Size = UDim2.new(0, 105, 0, 35)
    TabButton.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    TabButton.TextColor3 = Color3.fromRGB(200, 200, 200)
    TabButton.Text = name
    TabButton.Font = Enum.Font.SourceSansBold
    TabButton.TextSize = 15
    Instance.new("UICorner", TabButton).CornerRadius = UDim.new(0, 6)
    TabButton.Parent = TabList

    local Page = Instance.new("ScrollingFrame")
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.CanvasSize = UDim2.new(0, 0, 0, 400)
    Page.ScrollBarThickness = 4
    Page.Visible = false
    Page.Parent = ContentFrame
    
    local PageLayout = Instance.new("UIListLayout")
    PageLayout.Padding = UDim.new(0, 10)
    PageLayout.Parent = Page

    TabButton.MouseButton1Click:Connect(function()
        for _, t in pairs(tabs) do
            t.Page.Visible = false
            t.Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
            t.Btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        end
        Page.Visible = true
        TabButton.BackgroundColor3 = Color3.fromRGB(70, 80, 180)
        TabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    end)

    tabs[name] = {Btn = TabButton, Page = Page}
    return Page
end

-- --- UI UI COMPONENTS GENERATOR ---
local function AddToggle(parent, labelText, configKey)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -10, 0, 40)
    Row.BackgroundTransparency = 1
    Row.Parent = parent

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.7, 0, 1, 0)
    Label.Text = labelText
    Label.TextColor3 = Color3.fromRGB(230, 230, 230)
    Label.Font = Enum.Font.SourceSans
    Label.TextSize = 16
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    Label.Parent = Row

    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.25, 0, 0, 30)
    Button.Position = UDim2.new(0.75, 0, 0.5, -15)
    Button.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Text = "OFF"
    Button.Font = Enum.Font.SourceSansBold
    Button.TextSize = 14
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 6)
    Button.Parent = Row

    Button.MouseButton1Click:Connect(function()
        config.toggles[configKey] = not config.toggles[configKey]
        if config.toggles[configKey] then
            Button.BackgroundColor3 = Color3.fromRGB(60, 180, 60)
            Button.Text = "ON"
        else
            Button.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
            Button.Text = "OFF"
        end
    end)
end

local function AddTextBox(parent, labelText, placeholder, isDelay, configKey)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -10, 0, 40)
    Row.BackgroundTransparency = 1
    Row.Parent = parent

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.5, 0, 1, 0)
    Label.Text = labelText
    Label.TextColor3 = Color3.fromRGB(200, 200, 200)
    Label.Font = Enum.Font.SourceSans
    Label.TextSize = 15
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1
    Label.Parent = Row

    local Box = Instance.new("TextBox")
    Box.Size = UDim2.new(0.45, 0, 0, 30)
    Box.Position = UDim2.new(0.55, 0, 0.5, -15)
    Box.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    Box.TextColor3 = Color3.fromRGB(255, 255, 255)
    Box.Text = placeholder
    Box.Font = Enum.Font.SourceSans
    Box.TextSize = 14
    Instance.new("UICorner", Box).CornerRadius = UDim.new(0, 6)
    Box.Parent = Row

    Box.FocusLost:Connect(function()
        if isDelay then
            local num = tonumber(Box.Text)
            if num then config.delays[configKey] = num end
        else
            config.selections[configKey] = Box.Text
        end
    end)
end

-- --- BUILD CUSTOM PAGES ---
local PageCoins = CreateTab("Coins")
local PageShop = CreateTab("Shop")
local PageUpgrades = CreateTab("Upgrades")

-- Setup Coins Page
AddToggle(PageCoins, "Auto Collect Coins", "AutoCollect")
AddTextBox(PageCoins, "Coin Type:", "Basic Coin", false, "CollectCoinType")
AddTextBox(PageCoins, "Collect Delay (s):", "0.5", true, "AutoCollect")
AddToggle(PageCoins, "Auto Sell All", "AutoSell")
AddTextBox(PageCoins, "Sell Delay (s):", "2.0", true, "AutoSell")

-- Setup Shop Page
AddToggle(PageShop, "Auto Buy Coins", "AutoBuy")
AddTextBox(PageShop, "Coin Tier to Buy:", "Copper Coin", false, "BuyCoinType")
AddTextBox(PageShop, "Purchase Delay (s):", "1.0", true, "AutoBuy")

-- Setup Upgrades Page
AddToggle(PageUpgrades, "Auto Upgrade Luck", "AutoLuck")
AddTextBox(PageUpgrades, "Luck Delay (s):", "1.0", true, "AutoLuck")
AddToggle(PageUpgrades, "Auto Upgrade Value", "AutoValue")
AddTextBox(PageUpgrades, "Value Delay (s):", "1.0", true, "AutoValue")

-- Default to first Tab open
tabs["Coins"].Btn.BackgroundColor3 = Color3.fromRGB(70, 80, 180)
tabs["Coins"].Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
tabs["Coins"].Page.Visible = true

-- --- STABLE BACKGROUND ACTIONS LOOP ---
task.spawn(function()
    while true do
        if config.toggles.AutoCollect then
            Events.CoinLanded:FireServer(
                1.0807002921578,
                Vector3.new(-1165.0866699219, 0.72600001096725, -175.78570556641),
                config.selections.CollectCoinType,
                nil,
                nil
            )
            task.wait(config.delays.AutoCollect)
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    while true do
        if config.toggles.AutoSell then
            Events.SellAll:FireServer()
            task.wait(config.delays.AutoSell)
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    while true do
        if config.toggles.AutoBuy then
            Events.BuyCoin:FireServer(config.selections.BuyCoinType)
            task.wait(config.delays.AutoBuy)
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    while true do
        if config.toggles.AutoLuck then
            Events.RequestUpgrade:FireServer("Luck Multiplier")
            task.wait(config.delays.AutoLuck)
        else
            task.wait(0.2)
        end
    end
end)

task.spawn(function()
    while true do
        if config.toggles.AutoValue then
            Events.RequestUpgrade:FireServer("Value Multiplier")
            task.wait(config.delays.AutoValue)
        else
            task.wait(0.2)
        end
    end
end)
